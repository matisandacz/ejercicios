from heapq import heappop, heappush

# Mergear M arreglos ordenados en un solo arreglo ordenado.
# En O(n log M), n = cant total elem, m = cant arreglos
def merge(arr_of_arr):
	#Idea ok en pizarron. Ver como implementarlo.
	return
	

arr1 = [1,5,10]
arr2 = [4,8,13]
print(merge([arr1, arr2]))